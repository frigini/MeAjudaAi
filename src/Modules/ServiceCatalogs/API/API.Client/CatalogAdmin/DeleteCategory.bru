meta {
  name: Delete Category
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/catalogs/categories/{{categoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Delete Category
  
  Remove categoria (soft delete). Categoria não pode ter serviços associados.
  
  ## Parâmetros
  - `categoryId`: GUID da categoria
  
  ## Status: 204 No Content | 404 Not Found | 400 Bad Request
}
