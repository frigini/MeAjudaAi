// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { ApiActivatePost2Data, ApiActivatePost2Responses, ApiActivatePostData, ApiActivatePostResponses, ApiAllowedCitiesDeleteData, ApiAllowedCitiesDeleteErrors, ApiAllowedCitiesDeleteResponses, ApiAllowedCitiesGet2Data, ApiAllowedCitiesGet2Errors, ApiAllowedCitiesGet2Responses, ApiAllowedCitiesGetData, ApiAllowedCitiesGetResponses, ApiAllowedCitiesPatchData, ApiAllowedCitiesPatchErrors, ApiAllowedCitiesPatchResponses, ApiAllowedCitiesPostData, ApiAllowedCitiesPostErrors, ApiAllowedCitiesPostResponses, ApiAllowedCitiesPutData, ApiAllowedCitiesPutErrors, ApiAllowedCitiesPutResponses, ApiByCityGetData, ApiByCityGetErrors, ApiByCityGetResponses, ApiByEmailGetData, ApiByEmailGetErrors, ApiByEmailGetResponses, ApiByStateGetData, ApiByStateGetErrors, ApiByStateGetResponses, ApiByTypeGetData, ApiByTypeGetErrors, ApiByTypeGetResponses, ApiByUserGetData, ApiByUserGetErrors, ApiByUserGetResponses, ApiCategoriesDeleteData, ApiCategoriesDeleteResponses, ApiCategoriesGet2Data, ApiCategoriesGet2Errors, ApiCategoriesGet2Responses, ApiCategoriesGetData, ApiCategoriesGetResponses, ApiCategoriesPostData, ApiCategoriesPostResponses, ApiCategoriesPutData, ApiCategoriesPutResponses, ApiCategoryGetData, ApiCategoryGetResponses, ApiChangeCategoryPostData, ApiChangeCategoryPostResponses, ApiClientGetData, ApiClientGetResponses, ApiCspReportPostData, ApiCspReportPostResponses, ApiDeactivatePost2Data, ApiDeactivatePost2Responses, ApiDeactivatePostData, ApiDeactivatePostResponses, ApiDocumentsDeleteData, ApiDocumentsDeleteErrors, ApiDocumentsDeleteResponses, ApiDocumentsPostData, ApiDocumentsPostErrors, ApiDocumentsPostResponses, ApiProfilePutData, ApiProfilePutErrors, ApiProfilePutResponses, ApiProviderGetData, ApiProviderGetResponses, ApiProvidersDeleteData, ApiProvidersDeleteErrors, ApiProvidersDeleteResponses, ApiProvidersGet2Data, ApiProvidersGet2Errors, ApiProvidersGet2Responses, ApiProvidersGet3Data, ApiProvidersGet3Errors, ApiProvidersGet3Responses, ApiProvidersGetData, ApiProvidersGetErrors, ApiProvidersGetResponses, ApiProvidersPostData, ApiProvidersPostErrors, ApiProvidersPostResponses, ApiProvidersPutData, ApiProvidersPutErrors, ApiProvidersPutResponses, ApiRequestVerificationPostData, ApiRequestVerificationPostErrors, ApiRequestVerificationPostResponses, ApiRequireBasicInfoCorrectionPostData, ApiRequireBasicInfoCorrectionPostErrors, ApiRequireBasicInfoCorrectionPostResponses, ApiSearchGetData, ApiSearchGetResponses, ApiServicesDelete2Data, ApiServicesDelete2Responses, ApiServicesDeleteData, ApiServicesDeleteErrors, ApiServicesDeleteResponses, ApiServicesGet2Data, ApiServicesGet2Errors, ApiServicesGet2Responses, ApiServicesGetData, ApiServicesGetResponses, ApiServicesPost2Data, ApiServicesPost2Responses, ApiServicesPostData, ApiServicesPostErrors, ApiServicesPostResponses, ApiServicesPutData, ApiServicesPutResponses, ApiStatusGetData, ApiStatusGetErrors, ApiStatusGetResponses, ApiUploadPostData, ApiUploadPostErrors, ApiUploadPostResponses, ApiUsersDeleteData, ApiUsersDeleteErrors, ApiUsersDeleteResponses, ApiUsersGet2Data, ApiUsersGet2Errors, ApiUsersGet2Responses, ApiUsersGetData, ApiUsersGetErrors, ApiUsersGetResponses, ApiUsersPostData, ApiUsersPostErrors, ApiUsersPostResponses, ApiValidatePostData, ApiValidatePostResponses, ApiVerificationStatusGetData, ApiVerificationStatusGetErrors, ApiVerificationStatusGetResponses, ApiVerificationStatusPutData, ApiVerificationStatusPutErrors, ApiVerificationStatusPutResponses, ApiVerifyPostData, ApiVerifyPostErrors, ApiVerifyPostResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Listar todas as cidades permitidas
 *
 * Recupera todas as cidades permitidas (opcionalmente apenas as ativas)
 */
export const apiAllowedCitiesGet = <ThrowOnError extends boolean = false>(options?: Options<ApiAllowedCitiesGetData, ThrowOnError>) => (options?.client ?? client).get<ApiAllowedCitiesGetResponses, unknown, ThrowOnError>({ url: '/api/v1/admin/allowed-cities', ...options });

/**
 * Criar nova cidade permitida
 *
 * Cria uma nova cidade permitida para operações de prestadores (apenas Admin)
 */
export const apiAllowedCitiesPost = <ThrowOnError extends boolean = false>(options: Options<ApiAllowedCitiesPostData, ThrowOnError>) => (options.client ?? client).post<ApiAllowedCitiesPostResponses, ApiAllowedCitiesPostErrors, ThrowOnError>({
    url: '/api/v1/admin/allowed-cities',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Deletar cidade permitida
 *
 * Deleta uma cidade permitida
 */
export const apiAllowedCitiesDelete = <ThrowOnError extends boolean = false>(options: Options<ApiAllowedCitiesDeleteData, ThrowOnError>) => (options.client ?? client).delete<ApiAllowedCitiesDeleteResponses, ApiAllowedCitiesDeleteErrors, ThrowOnError>({ url: '/api/v1/admin/allowed-cities/{id}', ...options });

/**
 * Buscar cidade permitida por ID
 *
 * Recupera uma cidade permitida específica pelo seu ID
 */
export const apiAllowedCitiesGet2 = <ThrowOnError extends boolean = false>(options: Options<ApiAllowedCitiesGet2Data, ThrowOnError>) => (options.client ?? client).get<ApiAllowedCitiesGet2Responses, ApiAllowedCitiesGet2Errors, ThrowOnError>({ url: '/api/v1/admin/allowed-cities/{id}', ...options });

/**
 * Atualizar parcialmente cidade permitida
 *
 * Atualiza campos específicos de uma cidade permitida (Raio, Ativo)
 */
export const apiAllowedCitiesPatch = <ThrowOnError extends boolean = false>(options: Options<ApiAllowedCitiesPatchData, ThrowOnError>) => (options.client ?? client).patch<ApiAllowedCitiesPatchResponses, ApiAllowedCitiesPatchErrors, ThrowOnError>({
    url: '/api/v1/admin/allowed-cities/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Atualizar cidade permitida
 *
 * Atualiza uma cidade permitida existente
 */
export const apiAllowedCitiesPut = <ThrowOnError extends boolean = false>(options: Options<ApiAllowedCitiesPutData, ThrowOnError>) => (options.client ?? client).put<ApiAllowedCitiesPutResponses, ApiAllowedCitiesPutErrors, ThrowOnError>({
    url: '/api/v1/admin/allowed-cities/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retorna a configuração do cliente.
 * Apenas informações não-sensíveis são expostas.
 *
 * Retorna configurações não-sensíveis necessárias para o frontend (Keycloak, URLs, feature flags)
 */
export const apiClientGet = <ThrowOnError extends boolean = false>(options?: Options<ApiClientGetData, ThrowOnError>) => (options?.client ?? client).get<ApiClientGetResponses, unknown, ThrowOnError>({ url: '/api/configuration/client', ...options });

/**
 * Gerar URL de upload com SAS token
 *
 * Gera uma URL de upload com SAS token para envio direto ao Azure Blob Storage.
 *
 * **Fluxo:**
 * 1. Valida informações do documento
 * 2. Gera SAS token com 1 hora de validade
 * 3. Cria registro de documento no banco com status Uploaded
 * 4. Retorna URL de upload (com blob name e data de expiração)
 *
 * **Tipos de documentos suportados:**
 * - IdentityDocument: RG, CNH, CPF
 * - ProofOfResidence: Comprovante de residência
 * - CriminalRecord: Certidão de antecedentes
 * - Other: Outros documentos
 */
export const apiUploadPost = <ThrowOnError extends boolean = false>(options: Options<ApiUploadPostData, ThrowOnError>) => (options.client ?? client).post<ApiUploadPostResponses, ApiUploadPostErrors, ThrowOnError>({
    url: '/api/v1/documents/upload',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Consultar status de documento
 *
 * Retorna informações detalhadas sobre um documento específico.
 *
 * **Informações retornadas:**
 * - Status atual (Uploaded, PendingVerification, Verified, Rejected, Failed)
 * - Datas de upload e verificação
 * - Motivo de rejeição (se aplicável)
 * - Dados extraídos por OCR (se disponível)
 * - URLs de acesso ao documento
 */
export const apiStatusGet = <ThrowOnError extends boolean = false>(options: Options<ApiStatusGetData, ThrowOnError>) => (options.client ?? client).get<ApiStatusGetResponses, ApiStatusGetErrors, ThrowOnError>({ url: '/api/v1/documents/{documentId}/status', ...options });

/**
 * Listar documentos de um prestador
 *
 * Retorna todos os documentos associados a um prestador específico.
 *
 * **Casos de uso:**
 * - Visualizar todos os documentos enviados
 * - Verificar status de verificação de documentos
 * - Acompanhar progresso de validação de cadastro
 */
export const apiProviderGet = <ThrowOnError extends boolean = false>(options: Options<ApiProviderGetData, ThrowOnError>) => (options.client ?? client).get<ApiProviderGetResponses, unknown, ThrowOnError>({ url: '/api/v1/documents/provider/{providerId}', ...options });

/**
 * Solicitar verificação manual
 *
 * Solicita verificação manual de um documento quando OCR falha ou precisa validação adicional.
 *
 * **Quando usar:**
 * - OCR não conseguiu extrair dados do documento
 * - Documento foi rejeitado automaticamente mas precisa revisão
 * - Necessidade de validação humana adicional
 *
 * **Resultado:**
 * - Documento entra em fila de verificação manual
 * - Status alterado para PendingVerification
 * - Administrador será notificado para análise
 */
export const apiRequestVerificationPost = <ThrowOnError extends boolean = false>(options: Options<ApiRequestVerificationPostData, ThrowOnError>) => (options.client ?? client).post<ApiRequestVerificationPostResponses, ApiRequestVerificationPostErrors, ThrowOnError>({ url: '/api/v1/documents/{documentId}/request-verification', ...options });

/**
 * Aprovar ou rejeitar documento
 *
 * Aprova ou rejeita um documento após verificação manual.
 *
 * **Aprovar documento:**
 * ```json
 * {
 * "IsVerified": true,
 * "VerificationNotes": "Documento válido e legível"
 * }
 * ```
 *
 * **Rejeitar documento:**
 * ```json
 * {
 * "IsVerified": false,
 * "VerificationNotes": "Documento ilegível ou inválido"
 * }
 * ```
 *
 * **Requisitos:**
 * - Documento deve estar em status PendingVerification
 * - Apenas administradores podem executar esta ação
 */
export const apiVerifyPost = <ThrowOnError extends boolean = false>(options: Options<ApiVerifyPostData, ThrowOnError>) => (options.client ?? client).post<ApiVerifyPostResponses, ApiVerifyPostErrors, ThrowOnError>({
    url: '/api/v1/documents/{documentId}/verify',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Busca cidades/endereços para cadastro
 *
 * Retorna candidatos de localização baseados na query
 */
export const apiSearchGet = <ThrowOnError extends boolean = false>(options: Options<ApiSearchGetData, ThrowOnError>) => (options.client ?? client).get<ApiSearchGetResponses, unknown, ThrowOnError>({ url: '/api/v1/locations/search', ...options });

/**
 * Consultar prestadores paginados
 *
 * Recupera uma lista paginada de prestadores de serviços do sistema com suporte a filtros de busca.
 *
 * **Características:**
 * - 🔍 Busca por nome, tipo de serviço e status de verificação
 * - 📄 Paginação otimizada com metadados
 * - ⚡ Cache automático para consultas frequentes
 * - 🔒 Controle de acesso baseado em papéis
 * - 🌍 Restrição geográfica (piloto em cidades específicas)
 *
 * **Restrição geográfica (HTTP 451):**
 *
 * Este endpoint está sujeito a restrições geográficas durante a fase piloto.
 * O acesso é permitido apenas para usuários nas seguintes cidades:
 *
 * - **Muriaé** (MG) - IBGE: 3143906
 * - **Itaperuna** (RJ) - IBGE: 3302205
 * - **Linhares** (ES) - IBGE: 3203205
 *
 * A localização é determinada através dos headers HTTP:
 * - `X-User-City`: Nome da cidade
 * - `X-User-State`: Sigla do estado (UF)
 * - `X-User-Location`: Combinação "cidade|estado"
 *
 * Se o acesso for bloqueado, você receberá HTTP 451 com detalhes:
 * - Sua localização detectada
 * - Lista de cidades permitidas
 * - Códigos IBGE para validação
 *
 * **Parâmetros de busca:**
 * - `name`: Termo para filtrar prestadores por nome
 * - `type`: Filtro por tipo de serviço (ID numérico)
 * - `verificationStatus`: Status de verificação (ID numérico)
 * - `pageNumber`: Número da página (padrão: 1)
 * - `pageSize`: Tamanho da página (padrão: 10, máximo: 100)
 *
 * **Exemplos de uso:**
 * - Buscar prestadores: `?name=joão`
 * - Por tipo: `?type=1`
 * - Por status: `?verificationStatus=2`
 * - Paginação: `?pageNumber=2&pageSize=20`
 * - Combinado: `?name=médico&type=1&pageNumber=1&pageSize=10`
 */
export const apiProvidersGet = <ThrowOnError extends boolean = false>(options?: Options<ApiProvidersGetData, ThrowOnError>) => (options?.client ?? client).get<ApiProvidersGetResponses, ApiProvidersGetErrors, ThrowOnError>({ url: '/api/v1/providers', ...options });

/**
 * Processa requisição de criação de prestador de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Converte request em comando CQRS
 * 2. Envia comando através do dispatcher
 * 3. Processa resultado e retorna resposta HTTP apropriada
 * 4. Inclui localização do recurso criado no header
 */
export const apiProvidersPost = <ThrowOnError extends boolean = false>(options: Options<ApiProvidersPostData, ThrowOnError>) => (options.client ?? client).post<ApiProvidersPostResponses, ApiProvidersPostErrors, ThrowOnError>({
    url: '/api/v1/providers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Processa requisição de exclusão de prestador de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Valida ID do prestador e autorização administrativa
 * 2. Cria comando usando mapper ToDeleteCommand
 * 3. Envia comando através do dispatcher
 * 4. Processa resultado e retorna status apropriado
 * 5. Registra evento de auditoria (futuro)
 */
export const apiProvidersDelete = <ThrowOnError extends boolean = false>(options: Options<ApiProvidersDeleteData, ThrowOnError>) => (options.client ?? client).delete<ApiProvidersDeleteResponses, ApiProvidersDeleteErrors, ThrowOnError>({ url: '/api/v1/providers/{id}', ...options });

/**
 * Implementa a lógica de consulta de prestador por ID.
 *
 * Processo da consulta:
 * 1. Valida ID do prestador no formato GUID
 * 2. Cria query usando mapper ToQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com dados do prestador ou NotFound
 */
export const apiProvidersGet2 = <ThrowOnError extends boolean = false>(options: Options<ApiProvidersGet2Data, ThrowOnError>) => (options.client ?? client).get<ApiProvidersGet2Responses, ApiProvidersGet2Errors, ThrowOnError>({ url: '/api/v1/providers/{id}', ...options });

/**
 * Processa requisição de atualização de perfil de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Valida ID do prestador e autorização
 * 2. Converte request em comando CQRS
 * 3. Envia comando através do dispatcher
 * 4. Processa resultado e retorna resposta HTTP apropriada
 */
export const apiProvidersPut = <ThrowOnError extends boolean = false>(options: Options<ApiProvidersPutData, ThrowOnError>) => (options.client ?? client).put<ApiProvidersPutResponses, ApiProvidersPutErrors, ThrowOnError>({
    url: '/api/v1/providers/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Implementa a lógica de consulta de prestador por ID do usuário.
 *
 * Processo da consulta:
 * 1. Valida ID do usuário no formato GUID
 * 2. Cria query usando mapper ToUserQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com dados do prestador ou NotFound
 */
export const apiByUserGet = <ThrowOnError extends boolean = false>(options: Options<ApiByUserGetData, ThrowOnError>) => (options.client ?? client).get<ApiByUserGetResponses, ApiByUserGetErrors, ThrowOnError>({ url: '/api/v1/providers/by-user/{userId}', ...options });

/**
 * Implementa a lógica de consulta de prestadores por cidade.
 *
 * Processo da consulta:
 * 1. Valida parâmetro de cidade
 * 2. Cria query usando mapper ToCityQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com lista de prestadores
 */
export const apiByCityGet = <ThrowOnError extends boolean = false>(options: Options<ApiByCityGetData, ThrowOnError>) => (options.client ?? client).get<ApiByCityGetResponses, ApiByCityGetErrors, ThrowOnError>({ url: '/api/v1/providers/by-city/{city}', ...options });

/**
 * Implementa a lógica de consulta de prestadores por estado.
 *
 * Processo da consulta:
 * 1. Valida parâmetro de estado
 * 2. Cria query usando mapper ToStateQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com lista de prestadores
 */
export const apiByStateGet = <ThrowOnError extends boolean = false>(options: Options<ApiByStateGetData, ThrowOnError>) => (options.client ?? client).get<ApiByStateGetResponses, ApiByStateGetErrors, ThrowOnError>({ url: '/api/v1/providers/by-state/{state}', ...options });

/**
 * Implementa a lógica de consulta de prestadores por tipo.
 *
 * Processo da consulta:
 * 1. Valida enum de tipo do prestador
 * 2. Cria query usando mapper ToTypeQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com lista de prestadores
 */
export const apiByTypeGet = <ThrowOnError extends boolean = false>(options: Options<ApiByTypeGetData, ThrowOnError>) => (options.client ?? client).get<ApiByTypeGetResponses, ApiByTypeGetErrors, ThrowOnError>({ url: '/api/v1/providers/by-type/{type}', ...options });

/**
 * Implementa a lógica de consulta de prestadores por status de verificação.
 *
 * Processo da consulta:
 * 1. Valida enum de status de verificação
 * 2. Cria query usando mapper ToVerificationStatusQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com lista de prestadores
 */
export const apiVerificationStatusGet = <ThrowOnError extends boolean = false>(options: Options<ApiVerificationStatusGetData, ThrowOnError>) => (options.client ?? client).get<ApiVerificationStatusGetResponses, ApiVerificationStatusGetErrors, ThrowOnError>({ url: '/api/v1/providers/verification-status/{status}', ...options });

/**
 * Processa requisição de adição de documento de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Valida ID do prestador e autorização
 * 2. Converte request em comando CQRS
 * 3. Envia comando através do dispatcher
 * 4. Processa resultado e retorna prestador atualizado
 */
export const apiDocumentsPost = <ThrowOnError extends boolean = false>(options: Options<ApiDocumentsPostData, ThrowOnError>) => (options.client ?? client).post<ApiDocumentsPostResponses, ApiDocumentsPostErrors, ThrowOnError>({
    url: '/api/v1/providers/{id}/documents',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Processa requisição de remoção de documento de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Valida ID do prestador e autorização
 * 2. Cria comando usando mapper ToRemoveDocumentCommand
 * 3. Envia comando através do dispatcher
 * 4. Processa resultado e retorna prestador atualizado
 */
export const apiDocumentsDelete = <ThrowOnError extends boolean = false>(options: Options<ApiDocumentsDeleteData, ThrowOnError>) => (options.client ?? client).delete<ApiDocumentsDeleteResponses, ApiDocumentsDeleteErrors, ThrowOnError>({ url: '/api/v1/providers/{id}/documents/{documentType}', ...options });

/**
 * Processa requisição de atualização de status de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Valida ID do prestador e autorização administrativa
 * 2. Converte request em comando CQRS
 * 3. Envia comando através do dispatcher
 * 4. Processa resultado e retorna prestador atualizado
 * 5. Registra evento de auditoria (futuro)
 */
export const apiVerificationStatusPut = <ThrowOnError extends boolean = false>(options: Options<ApiVerificationStatusPutData, ThrowOnError>) => (options.client ?? client).put<ApiVerificationStatusPutResponses, ApiVerificationStatusPutErrors, ThrowOnError>({
    url: '/api/v1/providers/{id}/verification-status',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Processa requisição de solicitação de correção de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Valida ID do prestador e autorização
 * 2. Extrai identidade do usuário autenticado do contexto HTTP
 * 3. Converte request em comando CQRS com identidade verificada
 * 4. Envia comando através do dispatcher
 * 5. Processa resultado e retorna confirmação
 * 6. Emite evento de domínio para notificação
 */
export const apiRequireBasicInfoCorrectionPost = <ThrowOnError extends boolean = false>(options: Options<ApiRequireBasicInfoCorrectionPostData, ThrowOnError>) => (options.client ?? client).post<ApiRequireBasicInfoCorrectionPostResponses, ApiRequireBasicInfoCorrectionPostErrors, ThrowOnError>({
    url: '/api/v1/providers/{id}/require-basic-info-correction',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Processa requisição de remoção de serviço do provider.
 *
 * ### Remove um serviço do catálogo do provider
 *
 * **Funcionalidades:**
 * - ✅ Remove associação entre provider e serviço
 * - ✅ Emite evento de domínio ProviderServiceRemovedDomainEvent
 * - ✅ Valida que o provider oferece o serviço antes de remover
 *
 * **Campos obrigatórios:**
 * - providerId: ID do provider (UUID)
 * - serviceId: ID do serviço do catálogo (UUID)
 *
 * **Validações:**
 * - Provider deve existir
 * - Provider deve oferecer o serviço
 * - Provider não pode estar deletado
 */
export const apiServicesDelete = <ThrowOnError extends boolean = false>(options: Options<ApiServicesDeleteData, ThrowOnError>) => (options.client ?? client).delete<ApiServicesDeleteResponses, ApiServicesDeleteErrors, ThrowOnError>({ url: '/api/v1/providers/{providerId}/services/{serviceId}', ...options });

/**
 * Processa requisição de adição de serviço ao provider.
 *
 * ### Adiciona um serviço do catálogo ao provider
 *
 * **Funcionalidades:**
 * - ✅ Valida existência e status do serviço via IServiceCatalogsModuleApi
 * - ✅ Verifica se o serviço está ativo
 * - ✅ Previne duplicação de serviços
 * - ✅ Emite evento de domínio ProviderServiceAddedDomainEvent
 *
 * **Campos obrigatórios:**
 * - providerId: ID do provider (UUID)
 * - serviceId: ID do serviço do catálogo (UUID)
 *
 * **Validações:**
 * - Serviço deve existir no catálogo
 * - Serviço deve estar ativo
 * - Provider não pode já oferecer o serviço
 * - Provider não pode estar deletado
 */
export const apiServicesPost = <ThrowOnError extends boolean = false>(options: Options<ApiServicesPostData, ThrowOnError>) => (options.client ?? client).post<ApiServicesPostResponses, ApiServicesPostErrors, ThrowOnError>({ url: '/api/v1/providers/{providerId}/services/{serviceId}', ...options });

/**
 * Buscar prestadores de serviço
 *
 * Busca prestadores de serviço ativos com base em geolocalização e filtros.
 *
 * **Algoritmo de Busca:**
 * 1. Filtrar por raio a partir da localização de busca
 * 2. Aplicar filtros opcionais (serviços, avaliação, nível de assinatura)
 * 3. Classificar resultados por:
 * - Nível de assinatura (Platinum > Gold > Standard > Free)
 * - Avaliação média (maior primeiro)
 * - Distância (mais próximo primeiro)
 *
 * **Casos de Uso:**
 * - Encontrar prestadores próximos a uma localização específica
 * - Buscar prestadores que oferecem serviços específicos
 * - Filtrar por avaliação mínima ou nível de assinatura
 */
export const apiProvidersGet3 = <ThrowOnError extends boolean = false>(options: Options<ApiProvidersGet3Data, ThrowOnError>) => (options.client ?? client).get<ApiProvidersGet3Responses, ApiProvidersGet3Errors, ThrowOnError>({ url: '/api/v1/search/providers', ...options });

/**
 * Recebe e registra violações de CSP.
 */
export const apiCspReportPost = <ThrowOnError extends boolean = false>(options?: Options<ApiCspReportPostData, ThrowOnError>) => (options?.client ?? client).post<ApiCspReportPostResponses, unknown, ThrowOnError>({ url: '/api/csp-report', ...options });

/**
 * Listar todas as categorias
 *
 * Retorna todas as categorias de serviços do catálogo.
 *
 * **Filtros Opcionais:**
 * - `activeOnly` (bool): Filtra apenas categorias ativas (padrão: false)
 *
 * **Ordenação:**
 * - Categorias são ordenadas por DisplayOrder (crescente)
 *
 * **Casos de Uso:**
 * - Exibir menu de categorias para usuários
 * - Administração do catálogo de categorias
 * - Seleção de categoria ao criar serviço
 */
export const apiCategoriesGet = <ThrowOnError extends boolean = false>(options?: Options<ApiCategoriesGetData, ThrowOnError>) => (options?.client ?? client).get<ApiCategoriesGetResponses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/categories', ...options });

/**
 * Criar categoria de serviço
 *
 * Cria uma nova categoria de serviços no catálogo.
 *
 * **Validações:**
 * - Nome é obrigatório (máximo 100 caracteres)
 * - Descrição opcional (máximo 500 caracteres)
 * - DisplayOrder deve ser >= 0
 * - Nome deve ser único no sistema
 *
 * **Efeitos:**
 * - Categoria criada como ativa por padrão
 * - Pode receber serviços imediatamente
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiCategoriesPost = <ThrowOnError extends boolean = false>(options: Options<ApiCategoriesPostData, ThrowOnError>) => (options.client ?? client).post<ApiCategoriesPostResponses, unknown, ThrowOnError>({
    url: '/api/v1/service-catalogs/categories',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Deletar categoria de serviço
 *
 * Deleta uma categoria de serviços permanentemente.
 *
 * **Validações:**
 * - ID não pode ser vazio
 * - Categoria deve existir
 * - Categoria não pode ter serviços associados
 *
 * **Importante:** Operação destrutiva. Categorias com serviços não podem
 * ser deletadas. Use desativação ou mova os serviços primeiro.
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiCategoriesDelete = <ThrowOnError extends boolean = false>(options: Options<ApiCategoriesDeleteData, ThrowOnError>) => (options.client ?? client).delete<ApiCategoriesDeleteResponses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/categories/{id}', ...options });

/**
 * Buscar categoria por ID
 *
 * Retorna os detalhes completos de uma categoria específica.
 *
 * **Retorno:**
 * - Informações completas da categoria
 * - Status de ativação
 * - DisplayOrder para ordenação
 * - Datas de criação e atualização
 *
 * **Casos de Uso:**
 * - Exibir detalhes da categoria para edição
 * - Validar existência de categoria
 * - Visualizar informações completas
 */
export const apiCategoriesGet2 = <ThrowOnError extends boolean = false>(options: Options<ApiCategoriesGet2Data, ThrowOnError>) => (options.client ?? client).get<ApiCategoriesGet2Responses, ApiCategoriesGet2Errors, ThrowOnError>({ url: '/api/v1/service-catalogs/categories/{id}', ...options });

/**
 * Atualizar categoria de serviço
 *
 * Atualiza as informações de uma categoria existente.
 *
 * **Validações:**
 * - ID não pode ser vazio
 * - Categoria deve existir
 * - Nome é obrigatório (máximo 100 caracteres)
 * - Descrição opcional (máximo 500 caracteres)
 * - DisplayOrder deve ser >= 0
 *
 * **Nota:** Requer atualização completa (full-update pattern).
 * Todos os campos devem ser fornecidos.
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiCategoriesPut = <ThrowOnError extends boolean = false>(options: Options<ApiCategoriesPutData, ThrowOnError>) => (options.client ?? client).put<ApiCategoriesPutResponses, unknown, ThrowOnError>({
    url: '/api/v1/service-catalogs/categories/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ativar categoria de serviço
 *
 * Ativa uma categoria de serviços.
 *
 * **Efeitos:**
 * - Categoria fica visível em listagens públicas
 * - Permite criação de novos serviços nesta categoria
 * - Serviços existentes na categoria voltam a ser acessíveis
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiActivatePost = <ThrowOnError extends boolean = false>(options: Options<ApiActivatePostData, ThrowOnError>) => (options.client ?? client).post<ApiActivatePostResponses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/categories/{id}/activate', ...options });

/**
 * Desativar categoria de serviço
 *
 * Desativa uma categoria de serviços.
 *
 * **Efeitos:**
 * - Categoria não aparece em listagens públicas
 * - Impede criação de novos serviços nesta categoria
 * - Serviços existentes permanecem no sistema (soft-delete)
 *
 * **Nota:** Preferível à deleção quando há serviços associados.
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiDeactivatePost = <ThrowOnError extends boolean = false>(options: Options<ApiDeactivatePostData, ThrowOnError>) => (options.client ?? client).post<ApiDeactivatePostResponses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/categories/{id}/deactivate', ...options });

/**
 * Listar todos os serviços
 *
 * Retorna todos os serviços do catálogo.
 *
 * **Filtros Opcionais:**
 * - `activeOnly` (bool): Filtra apenas serviços ativos (padrão: false)
 *
 * **Casos de Uso:**
 * - Listar todo o catálogo de serviços
 * - Obter apenas serviços ativos para exibição pública
 * - Administração do catálogo completo
 */
export const apiServicesGet = <ThrowOnError extends boolean = false>(options?: Options<ApiServicesGetData, ThrowOnError>) => (options?.client ?? client).get<ApiServicesGetResponses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/services', ...options });

/**
 * Criar serviço
 *
 * Cria um novo serviço no catálogo.
 *
 * **Validações:**
 * - Nome é obrigatório (máximo 150 caracteres)
 * - Descrição opcional (máximo 1000 caracteres)
 * - DisplayOrder deve ser >= 0
 * - Categoria deve existir e estar ativa
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiServicesPost2 = <ThrowOnError extends boolean = false>(options: Options<ApiServicesPost2Data, ThrowOnError>) => (options.client ?? client).post<ApiServicesPost2Responses, unknown, ThrowOnError>({
    url: '/api/v1/service-catalogs/services',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Deletar serviço
 *
 * Deleta um serviço do catálogo permanentemente.
 *
 * **Validações:**
 * - ID não pode ser vazio
 * - Serviço deve existir
 * - Nenhum provedor pode estar oferecendo este serviço
 *
 * **Importante:** Operação destrutiva. Se provedores oferecem o serviço,
 * use desativação em vez de deleção para preservar dados históricos.
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiServicesDelete2 = <ThrowOnError extends boolean = false>(options: Options<ApiServicesDelete2Data, ThrowOnError>) => (options.client ?? client).delete<ApiServicesDelete2Responses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/services/{id}', ...options });

/**
 * Buscar serviço por ID
 *
 * Retorna os detalhes completos de um serviço específico.
 *
 * **Retorno:**
 * - Informações completas do serviço incluindo categoria
 * - Status de ativação
 * - Datas de criação e atualização
 *
 * **Casos de Uso:**
 * - Exibir detalhes do serviço para edição
 * - Visualizar informações completas do serviço
 * - Validar existência do serviço
 */
export const apiServicesGet2 = <ThrowOnError extends boolean = false>(options: Options<ApiServicesGet2Data, ThrowOnError>) => (options.client ?? client).get<ApiServicesGet2Responses, ApiServicesGet2Errors, ThrowOnError>({ url: '/api/v1/service-catalogs/services/{id}', ...options });

/**
 * Atualizar serviço
 *
 * Atualiza as informações de um serviço existente.
 *
 * **Validações:**
 * - ID não pode ser vazio
 * - Serviço deve existir
 * - Nome é obrigatório (máximo 150 caracteres)
 * - Descrição opcional (máximo 1000 caracteres)
 * - DisplayOrder deve ser >= 0
 *
 * **Nota:** Não altera a categoria do serviço. Use ChangeServiceCategory para isso.
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiServicesPut = <ThrowOnError extends boolean = false>(options: Options<ApiServicesPutData, ThrowOnError>) => (options.client ?? client).put<ApiServicesPutResponses, unknown, ThrowOnError>({
    url: '/api/v1/service-catalogs/services/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Listar serviços por categoria
 *
 * Retorna todos os serviços de uma categoria específica.
 *
 * **Parâmetros:**
 * - `categoryId` (route): ID da categoria
 * - `activeOnly` (query, opcional): Filtrar apenas serviços ativos (padrão: false)
 *
 * **Casos de Uso:**
 * - Exibir serviços disponíveis em uma categoria
 * - Listar ofertas por categoria para provedores
 * - Gestão de catálogo segmentado por categoria
 */
export const apiCategoryGet = <ThrowOnError extends boolean = false>(options: Options<ApiCategoryGetData, ThrowOnError>) => (options.client ?? client).get<ApiCategoryGetResponses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/services/category/{categoryId}', ...options });

/**
 * Alterar categoria do serviço
 *
 * Move um serviço para uma categoria diferente.
 *
 * **Validações:**
 * - Serviço deve existir
 * - Nova categoria deve existir e estar ativa
 * - Nova categoria não pode ser a mesma que a atual
 *
 * **Casos de Uso:**
 * - Reorganizar catálogo de serviços
 * - Corrigir categorização incorreta
 * - Adaptar estrutura de categorias
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiChangeCategoryPost = <ThrowOnError extends boolean = false>(options: Options<ApiChangeCategoryPostData, ThrowOnError>) => (options.client ?? client).post<ApiChangeCategoryPostResponses, unknown, ThrowOnError>({
    url: '/api/v1/service-catalogs/services/{id}/change-category',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ativar serviço
 *
 * Ativa um serviço, tornando-o disponível no catálogo.
 *
 * **Efeitos:**
 * - Serviço fica visível em listagens públicas
 * - Provedores podem adicionar este serviço às suas ofertas
 * - Serviço aparece em buscas de serviços ativos
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiActivatePost2 = <ThrowOnError extends boolean = false>(options: Options<ApiActivatePost2Data, ThrowOnError>) => (options.client ?? client).post<ApiActivatePost2Responses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/services/{id}/activate', ...options });

/**
 * Desativar serviço
 *
 * Desativa um serviço, removendo-o do catálogo ativo.
 *
 * **Efeitos:**
 * - Serviço não aparece em listagens públicas
 * - Provedores não podem adicionar este serviço a novas ofertas
 * - Serviço preserva dados históricos (soft-delete)
 *
 * **Nota:** Preferível à deleção quando provedores já oferecem o serviço.
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiDeactivatePost2 = <ThrowOnError extends boolean = false>(options: Options<ApiDeactivatePost2Data, ThrowOnError>) => (options.client ?? client).post<ApiDeactivatePost2Responses, unknown, ThrowOnError>({ url: '/api/v1/service-catalogs/services/{id}/deactivate', ...options });

/**
 * Validar múltiplos serviços
 *
 * Valida a existência e status de uma lista de serviços.
 *
 * **Funcionalidade:**
 * - Verifica se todos os IDs existem no catálogo
 * - Retorna quais serviços são válidos e quais são inválidos
 * - Indica serviços inativos separadamente
 *
 * **Casos de Uso:**
 * - Validar serviços antes de adicionar a um provedor
 * - Verificação em lote para importação de dados
 * - Garantir integridade referencial entre módulos
 *
 * **Permissões:** Requer privilégios de administrador
 */
export const apiValidatePost = <ThrowOnError extends boolean = false>(options: Options<ApiValidatePostData, ThrowOnError>) => (options.client ?? client).post<ApiValidatePostResponses, unknown, ThrowOnError>({
    url: '/api/v1/service-catalogs/services/validate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Processa requisição de consulta de usuários de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Extrai parâmetros de paginação da query string
 * 2. Cria query CQRS com parâmetros validados
 * 3. Envia query através do dispatcher
 * 4. Retorna resposta paginada estruturada com metadados
 *
 * Suporta parâmetros: PageNumber, PageSize, SearchTerm
 */
export const apiUsersGet = <ThrowOnError extends boolean = false>(options?: Options<ApiUsersGetData, ThrowOnError>) => (options?.client ?? client).get<ApiUsersGetResponses, ApiUsersGetErrors, ThrowOnError>({ url: '/api/v1/users', ...options });

/**
 * Processa requisição de criação de usuário de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Converte request em comando CQRS
 * 2. Envia comando através do dispatcher
 * 3. Processa resultado e retorna resposta HTTP apropriada
 * 4. Inclui localização do recurso criado no header
 */
export const apiUsersPost = <ThrowOnError extends boolean = false>(options: Options<ApiUsersPostData, ThrowOnError>) => (options.client ?? client).post<ApiUsersPostResponses, ApiUsersPostErrors, ThrowOnError>({
    url: '/api/v1/users',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Implementa a lógica de exclusão de usuário.
 *
 * Processo de exclusão:
 * 1. Valida ID do usuário no formato GUID
 * 2. Cria command usando mapper ToDeleteCommand
 * 3. Envia command através do dispatcher CQRS
 * 4. Retorna resposta HTTP 204 No Content
 */
export const apiUsersDelete = <ThrowOnError extends boolean = false>(options: Options<ApiUsersDeleteData, ThrowOnError>) => (options.client ?? client).delete<ApiUsersDeleteResponses, ApiUsersDeleteErrors, ThrowOnError>({ url: '/api/v1/users/{id}', ...options });

/**
 * Implementa a lógica de consulta de usuário por ID.
 *
 * Processo da consulta:
 * 1. Valida ID do usuário no formato GUID
 * 2. Cria query usando mapper ToQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com dados do usuário
 */
export const apiUsersGet2 = <ThrowOnError extends boolean = false>(options: Options<ApiUsersGet2Data, ThrowOnError>) => (options.client ?? client).get<ApiUsersGet2Responses, ApiUsersGet2Errors, ThrowOnError>({ url: '/api/v1/users/{id}', ...options });

/**
 * Implementa a lógica de consulta de usuário por email.
 *
 * Processo da consulta:
 * 1. Valida formato do email
 * 2. Cria query usando mapper ToEmailQuery
 * 3. Envia query através do dispatcher CQRS
 * 4. Retorna resposta HTTP com dados do usuário
 */
export const apiByEmailGet = <ThrowOnError extends boolean = false>(options: Options<ApiByEmailGetData, ThrowOnError>) => (options.client ?? client).get<ApiByEmailGetResponses, ApiByEmailGetErrors, ThrowOnError>({ url: '/api/v1/users/by-email/{email}', ...options });

/**
 * Processa requisição de atualização de perfil de usuário de forma assíncrona.
 *
 * Fluxo de execução:
 * 1. Valida ID do usuário no formato GUID
 * 2. Cria comando de atualização com dados da requisição
 * 3. Envia comando através do dispatcher CQRS
 * 4. Retorna resposta HTTP com dados atualizados
 *
 * Dados atualizáveis: FirstName, LastName, Email, PhoneNumber
 */
export const apiProfilePut = <ThrowOnError extends boolean = false>(options: Options<ApiProfilePutData, ThrowOnError>) => (options.client ?? client).put<ApiProfilePutResponses, ApiProfilePutErrors, ThrowOnError>({
    url: '/api/v1/users/{id}/profile',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
