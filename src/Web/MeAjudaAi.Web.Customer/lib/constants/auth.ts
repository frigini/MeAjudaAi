/**
 * Identity providers available in the system.
 * Used for social login hints (`kc_idp_hint`).
 */
export enum EAuthProvider {
    Google = "google",
    Facebook = "facebook",
    Apple = "apple",
    Microsoft = "microsoft"
}
